main()
	menu loop
		[C]ustom Folder Names
		[F]older Path Set
		[P]icture Dimension Set
		[A]uto crop White Space
		[D]elete folders
		[S]etup
		E[x]ecute program
		[E]xit
S()
	autocropToggle = false
	Input folder path = F(folder path)
	Autocrop white space? y/n
	if y
		autocropToggle = true
	else
		P(set picture dimension)
	Execute program

MVP:
Main()
	Menu loop
		[C]ustom Folder Names
		[F]older path set
		[P]icture dimension set
		[E]xecute Program